# ==============================================================================
#  SOVEREIGN OS DIM - Dependencies
# ==============================================================================
#  Application desktop 100% locale : aucun serveur HTTP, aucune socket.
#  Le frontend parle a Python via le pont in-process de pywebview.
# ==============================================================================

# Core - Fenetre native desktop avec frontend web
pywebview>=5.3.2

# Windows - runtime .NET requis par pywebview pour le renderer EdgeChromium.
# Sans pythonnet, pywebview ne peut pas creer de fenetre native sur Windows.
pythonnet==3.1.0rc0

# Build - Compilation en executable Windows (dev dependency)
pyinstaller>=6.11.1

# Import / export Excel (moulinette FICHCOMP, import CSV/Excel)
openpyxl>=3.1.0

# Generation des PDF (guides, dossier DSI) via fpdf2
fpdf2>=2.8.0

# Module ML (XGBoost stack) - inference locale, aucun appel reseau.
# format_detector (multi-classe), collision_risk + ddn_validity (binaires).
xgboost>=3.0.0
lightgbm>=4.5.0
scikit-learn>=1.6.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=18.0.0

# Dev dependencies (tests)
pytest>=8.3.4
pytest-cov>=6.0.0
