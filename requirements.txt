# ══════════════════════════════════════════════════════════════════════════════
#  SOVEREIGN OS DIM — Dependencies
# ══════════════════════════════════════════════════════════════════════════════
#  Generated: 2026-03-03
#  Project  : Sovereign OS V32.0 — Station DIM GHT Sud Paris
# ══════════════════════════════════════════════════════════════════════════════

# Core — Fenêtre native desktop avec frontend web
pywebview>=5.3.2

# Windows — Backend .NET requis par pywebview pour le renderer EdgeChromium
# Sans pythonnet, pywebview ne peut pas créer de fenêtre native sur Windows
pythonnet==3.1.0rc0

# Build — Compilation en exécutable Windows (dev dependency)
pyinstaller>=6.11.1

# HTTP — Pour d'éventuelles requêtes API externes
requests>=2.32.3

# ─── Bridge HTTP (intégration PHP) ────────────────────────────────────────
# Flask expose le moteur ATIH à une application PHP via HTTP JSON.
# Lancé par : python bridge.py
flask>=3.0.0

# ─── Import / visualisation Excel ─────────────────────────────────────────
# openpyxl lit les classeurs .xlsx côté bridge (endpoints /api/import-excel
# et /api/chart-from-excel consommés par la page PHP de visualisation).
openpyxl>=3.1.0

# ─── Génération du mode d'emploi PDF ──────────────────────────────────────
# fpdf2 (fork moderne de PyFPDF) — utilisé par tools/generate_manual.py
fpdf2>=2.8.0

# ─── FastAPI v2 (V37+, port 8766) ─────────────────────────────────────────
# API moderne typée Pydantic, doc Swagger auto sur /docs.
# Cohabite avec le bridge Flask historique sur port 8765.
# Lancement · uvicorn backend.fastapi_app:app --port 8766 --reload
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0  # client async + tests TestClient

# ─── Module ML (XGBoost stack, V36+) ──────────────────────────────────────
# Trois modèles entraînés sur dataset synthétique ATIH 2000-2026 ·
# format_detector (multi-classe), collision_risk + ddn_validity (binaires).
# Voir backend/ml/ pour le pipeline d'entraînement et l'inférence.
xgboost>=3.0.0
lightgbm>=4.5.0
scikit-learn>=1.6.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=18.0.0  # parquet · cache du dataset synthétique

# ─── Dev Dependencies (tests) ─────────────────────────────────────────────
pytest>=8.3.4
pytest-cov>=6.0.0
